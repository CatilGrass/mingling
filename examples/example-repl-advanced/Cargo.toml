[package]
name = "example-repl-advanced"
version = "0.1.0"
edition = "2024"

[dependencies.mingling]
path = "../../mingling"
features = ["repl", "repl_extra", "parser", "extra_macros"]

[dependencies]
just_fmt = "0.1.2"
